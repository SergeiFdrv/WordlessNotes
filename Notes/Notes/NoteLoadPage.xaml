<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Notes.NotePickPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Delete" Clicked="Delete_Clicked"/>
        <ToolbarItem Text="Open" Clicked="Open_Clicked"/>
    </ContentPage.ToolbarItems>
    <ListView x:Name="MyListView"
            CachingStrategy="RecycleElement">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Padding="4" Spacing="0">
                        <Label Text="{Binding Name}" TextColor="Black" FontSize="14" LineBreakMode="MiddleTruncation"/>
                        <Label Text="{Binding Path}" TextColor="Gray" FontSize="12" LineBreakMode="MiddleTruncation"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
