<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:customrenderer="clr-namespace:Notes.Renderers"
             mc:Ignorable="d"
             x:Class="Notes.Views.CustomView">
    <ContentView.Content>
        <Grid HorizontalOptions="CenterAndExpand" Focused="Item_Focused">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="270"/>
                <ColumnDefinition Width="35"/>
            </Grid.ColumnDefinitions>
            <StackLayout x:Name="meat" Spacing="0" Grid.Column="0">
                <BoxView x:Name="Img" BackgroundColor="DarkOrchid" Margin="0" Focused="Item_Focused"/>
                <!-- Изучить плагин для вставки изображений: https://github.com/jamesmontemagno/MediaPlugin -->
                <customrenderer:TextEditor x:Name="TextEditor" Placeholder="Paragraph" PlaceholderColor="Gray" Focused="Item_Focused"/>
                <ListView x:Name="List" IsVisible="False" VerticalOptions="Start" HasUnevenRows="True">
                    <ListView.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>List item</x:String>
                            <x:String>List item</x:String>
                            <x:String>List item</x:String>
                            <x:String>List item</x:String>
                            <x:String>List item</x:String>
                            <x:String>List item</x:String>
                            <x:String>List item</x:String>
                            <x:String>List item</x:String>
                        </x:Array>
                    </ListView.ItemsSource>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Text="-" VerticalOptions="Start" VerticalTextAlignment="Center" Margin="10, 5, 0, 0"/>
                                    <customrenderer:TextEditor Grid.Column="1" VerticalOptions="Center" Placeholder="{Binding}" PlaceholderColor="Gray"/>
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
            <Button x:Name="XBtn" Text="X" HeightRequest="35" Clicked="Button_Clicked" Grid.Column="1"/>
        </Grid>
    </ContentView.Content>
</ContentView>