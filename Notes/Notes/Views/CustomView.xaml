<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:customrenderer="clr-namespace:Notes.Renderers"
             xmlns:customview="clr-namespace:Notes.Views"
             mc:Ignorable="d"
             x:Class="Notes.Views.CustomView">
    <ContentView.Content>
        <Grid HorizontalOptions="CenterAndExpand" Focused="Item_Focused">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="25"/>
            </Grid.ColumnDefinitions>
            <StackLayout x:Name="meat" Spacing="0" Grid.Column="0">
                <Image x:Name="Img" BackgroundColor="DarkOrchid" Margin="0" Focused="Item_Focused" IsVisible="False">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="Image_Tapped"/>
                    </Image.GestureRecognizers>
                </Image>
                <!-- Изучить плагин для вставки изображений: https://github.com/jamesmontemagno/MediaPlugin -->
                <customrenderer:TextEditor x:Name="TextEditor" Placeholder="Paragraph" PlaceholderColor="Gray" Focused="Item_Focused"/>
                <customview:CustomListView x:Name="List" IsVisible="False"/>
            </StackLayout>
            <Button x:Name="XBtn" Text="X" HeightRequest="35" Clicked="Button_Clicked" Grid.Column="1"/>
        </Grid>
    </ContentView.Content>
</ContentView>